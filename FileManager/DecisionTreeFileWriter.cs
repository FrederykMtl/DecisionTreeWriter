using DTO;

namespace FileManager
{
    public class DecisionTreeFileWriter
    {
        private UserConfigurations _configurations;

        public DecisionTreeFileWriter(UserConfigurations configurations)
        {
            _configurations = configurations;
        }

        public bool SaveTree(Node rootNode)
        {

            return false;
        }
    }
}